
const circle = require('./circle');

const radius = 5;

console.log(`Circumference of circle with radius ${radius}: ${circle.circumference(radius)}`);
console.log(`Area of circle with radius ${radius}: ${circle.area(radius)}`);
